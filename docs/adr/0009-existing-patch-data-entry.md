---
status: accepted
---

# Use only the existing patch data package

The sole valid data entry is the existing, already split patch data package.
Complete WSI files, candidate generation from WSI files, and any claim that the
available patches cover a complete slide or all tissue regions are outside the
project scope. Group- or slide-level aggregation may use only identifiers and
patch rows already present in the validated immutable manifest.
