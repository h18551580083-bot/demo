# P0 决策状态

已由现有数据与开发文档锁定：

- 数据集：CAM16 patch，RGB 256×256，normal/tumor 二分类。
- 划分：使用数据包既有 train/val/test；以 `slide_id` 作为患者/WSI 隔离键。
- 测试集：仅用于锁定方案后的 final-once 评价。
- 接口：数字理想与 4f 等效模式必须共享输出维度、通道顺序及核哈希。
- 仿真边界：所有结果表述为数字或光学等效仿真，不表述为硬件实测。

仍需研究负责人在正式 P1 前锁定：

- RGB/灰度/染色分解输入方案；
- 核族、尺度、方向数、通道预算及公平基线；
- 探测后处理与电子后端参数预算；
- P4 seed、训练预算、置信区间与 final-once 启用条件；
- P5 目标数据集、物理尺度适配、迁移模式和 L2/L3 主张门。

